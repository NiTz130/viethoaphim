# OCR Upgrade (2026 stack) — COMPLETE

Run on: 2026-06-30
Operator: (user)
Stack under test: paddlepaddle 3.x + numpy 2.x + torch 2.6+

## Status

ACCEPTABLE — Spec 1 (gitleaks infra + setup hardening), Spec 2 (high-priority fixes), and Spec 3 (numpy stack upgrade + code quality refactor) all shipped. The OCR regression harness is in place via `make ocr-diff` (see Makefile).

## Cutover Commits

- `bcfc258` — `build(cutover-3a): rewrite pyproject.toml ML stack with hybrid pins; shim requirements-ocr.txt`
- `978b0dc` — `fix(deps): pin paddleocr<4 in pyproject.toml + requirements-2026.txt`
- `12a2139` — `build(security): add gitleaks pre-commit + CI scanning; ship .env.example` (Spec 1)
- `79d16b0` — `refactor(translate): split 671-line file into 5-file subpackage` (Spec 3, M1)

## Top 10 diff segments

See `tests/fixtures/ocr_diff_report.json` for the latest regression run output (generated by `make ocr-diff` or `make ocr-diff-2026`).
